Phase one. do we have a BIC plot or somrthing that visually implies 4 clusters is sensible? 

Phase 2:
1. i suspect the peaks at 10, 20 ad 30 econds stem from the fact that we defined an event as a ten second slice. this should be mentioned otherwise it looks that there are three distince regimes in brkingduration

2. try more classifiers. there are so many datapoints its worthwhil to have some more of them. good performance of decision tree is good, so we have one high scoring interpretable classifiere but which model works best?

3. did we try LDA with PCA or just with  the sensors, using the first coule PCA could prevent it from having to many parameter

4. plots for breaking intensity (denisty) could use a different scale , even if some observations are omitted. th bell curve is hard to see. even more bukets for the histogram would be good.

5. regression plots for brake intensity would be nice. to see some linear dependence, also maybe consider using PCA regressio  here if to many sensors are significant. some variable selection methods should be ussed

6. the last plot and section: predicting failure is very confusing and hard to read the whole section needs to explain in more detail what we do here. this looks like voodoo