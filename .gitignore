# Data (large, redistributable from DOI 10.48436/9ja0q-bq581)
train/
test/
data/raw/
data/interim/
# Derived processed data (4.7 GB, regenerable from the scripts) — not tracked
data/processed/

# Coverage
.coverage

# Python
__pycache__/
*.pyc
.venv/

# Outputs kept in repo: results/, models/ (small artifacts)
# Oversized regenerable models (each > GitHub's 100 MB limit); regenerated by the
# pipeline. The importances/metrics they produce are tracked as CSVs under results/.
models/phase1/rf_feature_importance.joblib
models/phase2/decel_regress_rf_deceleration.joblib
models/phase2/decel_regress_rf_mean.joblib
models/phase2/decel_regress_rf_peak.joblib
